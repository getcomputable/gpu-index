# Mint record: b300 (annex_a_v0_2_calc_v7)

The full mint prose for `config/index_panel_b300.json`, moved here
verbatim from that config's `calc.description` (audit prose; it is
never embedded in artifact bytes). METHODOLOGY.md is the binding
methodology document -- this text is the mint's summary of record.

calc_v7 (hourly panel mint; METHODOLOGY.md is the binding methodology document -- this text is the summary). The index and its weights recompute at EVERY hourly observation (rule A1); the fallback->dynamic weight switch adopts the 50% attendance floor over scheduled observations (rule A2, dynamic_weights.attendance_floor); vast stays LOWEST-ELIGIBLE for now (rule A3 -- no statistic named on the seat; the B200 lane's population-gated VWM is deliberately not adopted here); doc fixes land with this change (rule A4). RECORD STITCHING: basket-lane 4-slot snapshots (04/10/16/22Z, index/b300_basket) through 2026-08-23, raw-observatory hourly snapshots (index/raw_observatory) from 2026-08-24T00Z -- record_sources and the era-scoped slot_grids both ride calc_params under the D2 refuse-to-extend fence, so a record or cadence change is a mint. NO PARITY WITH THE DAILY SERIES: no byte-parity with the frozen annex_a_v0_2_calc_v6 daily series is possible or claimed, and no fallback_parity key exists on panel lanes -- the hourly series advances filter windows and weights at every observation (4/day pre-cutover, 24/day after) where the daily series advanced once per day, so values at a shared wall-clock hour legitimately diverge; that divergence IS rule A1, the same doctrine as the v6 mint. Everything else carries over from calc_v6 unchanged: the recorded-currency 3.0-sigma filter with 0.05 floor (rule D1 fail-closed currency handling), the median-of-CI-votes aggregate, manual-exclusion pinning per published observation (date-scoped entries hold out every observation of their date -- the shape the carried-over entries below need), the L5 jump screen now at calc with the verdict as artifact data, and the predictive weight scheme minus the retired risk caps. The v1-v6 daily series stay frozen under their own keyspaces; v7 replays from its own genesis; any param change touching a published v7 observation mints v8. TIER SCREEN: eligibility is the ALLOW-LIST calc.eligible_tiers, on-demand only -- methodology section 5 as written, replacing the retired exclusion-list interruptible_tiers screen. This hourly mint RECONCILES the recorded committed-tier limitation (the hourly-mint tier reconciliation): the frozen daily calc_v6 series kept committed/reserved rates eligible (a committed rate could win lowest-eligible) and retains that behavior in its frozen bytes; this lane admits on-demand rows alone, so latitude's print is its on-demand rate from genesis in the v7 replay (its monthly-commit row is tier-ineligible), reserved 'Starting from' floors and reserved tenors never print, and gmicloud's from_floor and fal's serverless rows remain out by tier. MEMBER EXTRA SCREEN: runpod's observatory record carries Secure AND Community cloud rows under one sku_identifier, distinguished only by the collector's structured extra.cloud field -- the seat's extra_require admits cloud 'secure' only (the membership rule excludes Community; basket-era rows carry no extra dict and pass, that era being secure-only by construction). OPEN METHODOLOGY CALLS (recorded, non-blocking): window/warm-up/currency-confirm counts now span hours (20h/10h/3h) -- resizing is a recorded outstanding mint; whether this lane's vast seat should adopt the B200 population-gated statistic. AMENDED INTO THIS MINT BEFORE ANY OBSERVATION PUBLISHED (perf review 2026-08-23): (a) drift_scan_observations moved out of calc to a TOP-LEVEL operational key -- the drift-scan bound gates an ops sweep, never the series bytes (the fallback_parity_methodology_id precedent), so it no longer rides calc_params under the D2 fence, and the scan runs on the 16:00Z firing (or --drift-scan) instead of every firing; (b) replay is BOUNDED: state rebuilds from the trailing state window (dw history + max forward horizon + prune margin -- the weight-state prune bound, inside which every consulted state fully rebuilds) behind the publish frontier, seeded by the latest pre-window artifact for the weight-mode latch and the D2 baseline, and the manual-exclusion pin check covers published artifacts inside that window only (older artifacts are immutable; their pins were enforced when the frontier passed them); (c) the ECB feed is fetched only when an unpublished observation day cannot resolve a stored rate after walk-back (stored FX reads bounded to the window plus the staleness allowance). AMENDED INTO THIS MINT BEFORE ANY OBSERVATION PUBLISHED (harden review 2026-08-23): (d) the fallback->dynamic switch quorum additionally requires a NON-EMPTY attendance-passer set -- with zero passers (every candidate below the attendance floor, a lane-wide outage's aftermath) the every-passer-has-Q clause is vacuously true and one post-outage observation could have flipped the PERMANENT latch on zero attendance information, inverting the A2 intent; genesis is unaffected (a zero-scheduled window makes every candidate a passer at ratio 1.0); (e) row eligibility fails closed on NON-FINITE prices: a candidate row must carry a finite native price (and a finite USD price when one is present) or it is excluded and counted (screen_counts non_finite_price) -- json admits NaN/Infinity literals and non-USD rows skip the capture-side USD plausibility band, so a non-finite native could otherwise poison the lowest-eligible min; capture-side normalization now also flags non-finite natives implausible (flag-only, future snapshots only); (f) a new top-level record_exclusions key ([{date, hour, reason}]) quarantines a poisoned/unparseable record object at an unpublished stamp: the observation publishes an explicit record_quarantined artifact (index null, observation_missed false) WITHOUT reading the record -- instead of the lane wedging forever behind publish-in-order -- and the key rides calc_params, pinned per published observation exactly like manual_exclusions. Artifact-visibility hardening in the same review: silent ok-status seats pin eligible_rows plus per-screen screen_counts (a seat whose rows ALL screen out is distinguishable from one with no rows at all -- the dead-seat tripwire), and an observation_missed artifact publishes only after a confirming FRESH re-LIST of the slot keys (a transient empty LIST must never pin an immutable false missed record).
